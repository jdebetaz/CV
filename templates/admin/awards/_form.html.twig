
{{ form_start(form, {attr: {class: 'stacked'}}) }}
<div class="grid">
  {{ form_row(form.title) }}
  {{ form_row(form.company) }}
  {{ form_row(form.recievedAt) }}
  <div class="full">
    <button type="submit" class="btn btn-success">Sauvegarder</button>
  </div>
</div>
{{ form_end(form) }}