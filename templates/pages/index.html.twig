{% extends 'base.html.twig' %}

{% block title %}
    CV - Jonathan Debétaz
{% endblock %}

{% block body %}
    <div class="sidebar">
        <div class="profile">
            <img src="/images/profile.jpg" alt="profile picture" class="avatar">
            <h1 class="name">Jonathan Debétaz</h1>
            <h3>{{ group.title }}</h3>
        </div>
        <ul class="contact-list">
            <li class="birthday">
                <i class="fas fa-birthday-cake"></i>
                <span>{{ "1996-08-21" | format_datetime(pattern="dd MMMM Y", locale="fr_FR") }} ({{ today | date_modify("-1996 years") | date("Y") | number_format }} ans)</span>
            </li>
            <li class="email">
                <i class="fas fa-envelope"></i>
                <a href="mailto:contact@jonthan-d.ch">contact@jonthan-d.ch</a>
            </li>
            <li class="phone">
                <i class="fas fa-phone"></i>
                <a href="tel:079 136 79 47">079 136 79 47</a>
            </li>
            <li class="website">
                <i class="fas fa-globe-americas"></i>
                <a href="https://jonathan-d.ch" target="_blank">jonathan-d.ch</a>
            </li>
            <li class="linkedin">
                <i class="fab fa-linkedin"></i>
                <a href="https://linkedin.com/in/jdebetaz" target="_blank">jdebetaz</a>
            </li><li class="github">
                <i class="fab fa-github"></i>
                <a href="https://github.com/jdebetaz" target="_blank">jdebetaz</a>
            </li>
        </ul>
        <h2>Langues</h2>
        <ul class="languages">
            {% for lang in languages %}
                <li>{{ lang.title }} <span>({{lang.level}})</span></li>
            {% endfor %}
        </ul>
        <h2>Compétences</h2>
        {% for category in group.categories %}
            <div>
                <h3>{{ category.title }}</h3>
                <ul class="skills">
                    {% for skill in category.skills %}
                        <li>{{ skill.title}}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endfor %}
        <h2>Hobbies</h2>
        {% for hobby in hobbies %}
            <div>
                <h3>{{ hobby.title }}</h3>
                <ul class="skills">
                    {% for tag in hobby.skills %}
                        <li>{{ tag.title}}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endfor %}
    </div>
    <div class="content">
        <h2><i class="fas fa-user fa-xs"></i>Profile</h2>
        <div class="summary">
            <p>Passionné par les nouvelles technologies, je cherche constamment à apprendre davantage pour apporter des solutions innovantes et créatives.<br>Autodidacte et audacieux, j’aime relever de nouveaux défis et travailler en équipe.</p>
        </div>
        <h2><i class="fas fa-briefcase fa-xs"></i> Expériences</h2>
        {% for experience in experiences %}
            <div class="item">
                <div class="meta">
                    <div class="upper-row">
                        <h3 class="job-title">{{ experience.job }}</h3>
                        <div class="time">{{ experience.startAt | format_datetime(pattern="MMMM Y", locale="fr_FR") | capitalize }} - {{ experience.endAt | format_datetime(pattern="MMMM Y", locale="fr_FR") | capitalize }}</div>
                    </div>
                    <div class="company">{{ experience.company }}</div>
                </div>
                <div class="details">
                    {{ experience.content }}
                </div>
            </div>
        {% endfor %}
        <h2><i class="fas fa-graduation-cap fa-xs"></i> Formations</h2>
        {% for education in educations %}
            <div class="item">
                <div class="meta">
                    <div class="upper-row">
                        <h3 class="job-title">{{ education.degree }}</h3>
                        <div class="time">{{ education.startAt | format_datetime(pattern="MMMM Y", locale="fr_FR") | capitalize }} - {{ education.endAt | format_datetime(pattern="MMMM Y", locale="fr_FR") | capitalize }}</div>
                    </div>
                    <div class="company">{{ education.school }}</div>
                </div>
            </div>
        {% endfor %}
        <h2><i class="fas fa-award fa-xs"></i> Prix & Certificats</h2>
        {% for award in awards %}
            <div class="item">
                <div class="meta">
                    <div class="upper-row">
                        <h3 class="job-title">{{ award.title }}</h3>
                        <div class="time">{{ award.recievedAt | format_datetime(pattern="MMMM Y", locale="fr_FR") | capitalize }}</div>
                    </div>
                    <div class="company">{{ award.company }}</div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
